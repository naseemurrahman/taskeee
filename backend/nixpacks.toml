[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --omit=dev"]

[phases.build]
cmds = []

[start]
cmd = "(node src/utils/migrate.js || echo 'Migrations failed, starting server in degraded mode') && node src/server.js"
