[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --omit=dev"]

[phases.build]
cmds = []

[start]
cmd = "node src/utils/migrate.js || echo 'Migration step failed; continuing startup' && node src/server.js"
