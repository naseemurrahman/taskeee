{
  "build": {
    "builder": "nixpacks",
    "nixpacks": {
      "aptPackages": []
    }
  },
  "deploy": {
    "command": "npm run migrate && npm start",
    "restartPolicyType": "Always",
    "restartPolicyMaxRetries": 10
  }
}
