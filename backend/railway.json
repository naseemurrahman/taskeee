{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node src/utils/migrate.js && node src/server.js",
    "restartPolicyType": "Always",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
