[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install --omit=dev"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && node scripts/migrate.js && node src/server.js"
